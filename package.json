{
  "name": "onnxruntime-web-tutorial",
  "version": "1.0.0",
  "description": "Run ONNX models in the browser",
  "main": "src/js/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx webpack",
    "serve": "npm run build && npx light-server -s . -p 8888",
    "format": "npx prettier --write src"
  },
  "author": "Max Kraan",
  "license": "MIT",
  "dependencies": {
    "fs": "^0.0.1-security",
    "light-server": "^2.9.1",
    "onnxruntime-web": "^1.14.0"
  },
  "devDependencies": {
    "copy-webpack": "^2.0.5",
    "copy-webpack-plugin": "^8.1.1",
    "webpack": "^5.82.0",
    "webpack-cli": "^4.10.0"
  }
}
